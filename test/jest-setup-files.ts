import { setConfig } from "next/config"
// next-compose-plugins and all that madness in next.config.js makes it hard to import that config cleanly.
// So, although its not ideal, we're just going to hard-code our runtimeconfig values for now.
setConfig({})
